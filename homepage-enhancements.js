document.addEventListener('DOMContentLoaded',()=>{const slider=document.querySelector('[data-industry-slider]');if(!slider)return;const track=slider.querySelector('.industry-track');const slides=[...slider.querySelectorAll('.industry-slide')];const dots=slider.querySelector('.industry-dots');const prev=slider.querySelector('.industry-prev');const next=slider.querySelector('.industry-next');let index=0;let timer;slides.forEach((_,i)=>{const b=document.createElement('button');b.type='button';b.className='industry-dot'+(i===0?' active':'');b.setAttribute('aria-label',`Go to industry slide ${i+1}`);b.addEventListener('click',()=>go(i,true));dots.appendChild(b)});const dotEls=[...dots.children];function go(i,manual=false){index=(i+slides.length)%slides.length;track.style.transform=`translateX(-${index*100}%)`;dotEls.forEach((d,n)=>d.classList.toggle('active',n===index));if(manual)restart()}function restart(){clearInterval(timer);timer=setInterval(()=>go(index+1),6000)}prev?.addEventListener('click',()=>go(index-1,true));next?.addEventListener('click',()=>go(index+1,true));slider.addEventListener('mouseenter',()=>clearInterval(timer));slider.addEventListener('mouseleave',restart);let startX=0;slider.addEventListener('touchstart',e=>{startX=e.touches[0].clientX},{passive:true});slider.addEventListener('touchend',e=>{const dx=e.changedTouches[0].clientX-startX;if(Math.abs(dx)>45)go(index+(dx<0?1:-1),true)},{passive:true});restart()});